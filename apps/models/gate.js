class gates {
    title;
    stages;
    journey;
    sortOrder;
    createdAt;
    updateAt;
}

module.exports = gates;


// class gate {
//     constructor(title, description, journey, stages, sortOrder, createdAt, updatedAt) {
//         this.title = title;
//         this.description = description;
//         this.journey = journey;
//         this.stages = stages;
//         this.sortOrder = sortOrder; // Sử dụng giá trị tự động tính toán
//         this.createdAt = createdAt;
//         this.updatedAt = updatedAt; // Gán updatedAt khi tạo
//     }
// }

// module.exp